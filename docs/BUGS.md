# Bugs resueltos

## E02 · Panel de evento mensual

1. **ScenarioBar solo en desarrollo** — resuelto.
2. **`key` prop documentada como deuda técnica** — resuelto.
3. **Stub de `MEMBERS` documentado con query Supabase** — resuelto.
4. **Stub de `CONFIRMATIONS_INIT` documentado con query Supabase** — resuelto.
5. **`setTimeout` de `CrearEvento` y `EditEvento` documentados como stubs de Supabase** — resuelto.
6. **`Notificando` documentado como stub de server action** — resuelto.
7. **Simulación realtime documentada con patrón `supabase.channel()`** — resuelto.
8. **Botón Compartir resumen con `onClick` stub** — resuelto.
9. **Tokens `tertiaryContainer`, `warningContainer`, `onSecondaryFixed` incorporados** — resuelto.
10. **JSDoc agregado en componentes y funciones públicas del archivo** — resuelto.
